<?xml version="1.0" encoding="utf-8"?>
<!--
    Since Android 9 the platform blocks plain HTTP by default, and it is right to.

    campus.sma-techno.net is served over HTTPS with a valid Let's Encrypt certificate, so it
    needs no exception here and gets none: the app carries tokens granting access to a
    child's location history, and over cleartext anyone on the same wifi can read them.

    The only exceptions are the loopback addresses a developer uses against a machine on
    their own desk. 10.0.2.2 is the Android emulator's alias for the host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
